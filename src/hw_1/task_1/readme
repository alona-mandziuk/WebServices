1. Створити XML файл та до нього відповідну схему XSD:
    Банківські вклади.
    — Name — назва вкладу;
    — Country — країна реєстрації;
    — Type — тип вкладу (до запитання, строковий, розрахунковий, накопичувальний, ощадний, в металі);
    — Depositor — ім'я вкладника;
    — Account id — номер рахунку;
    — Amount on deposit — сума вкладу;
    — Profitability — річний відсоток;
    — Time constraints — термін вкладу
    Кореневий елемент назвати Bank.
2. При розробці XSD використати прості та комплексні типи, перерахування, шаблони та граничні значення.
3. Згенерувати клас, відповідно даному опису.
4. Створити додаток для розбору XML-документу та ініціалізації колекції об'єктів інформацією з XML-файлу.
    Для розбору використати SAX, DOM і StAX парсери. Для сортування об'єктів використати інтерфейс Comparator.
5. Виконати перевірку XML-документу за допомоги XSD.
6. Виявити метод, який виконує перетворення за допомогою XSL розробленого XML документа
    в PDF з виводом інформації а табличному вигляді.
